/**
 * 네이버 블로그 자동화 서비스
 * Playwright 기반 네이버 블로그 에디터 자동화
 *
 * content-processor.ts에서 분리됨
 */

import '@/shared/types/electron.types';

export class NaverBlogAutomationService {
  // 이 파일은 content-processor.ts의 Playwright 함수들을 담을 예정
  // 현재는 Step3(발행) 단계에서 사용되므로 나중에 Step3 리팩토링 시 이동 예정
}
